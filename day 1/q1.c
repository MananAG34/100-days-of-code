// Write a program to input two numbers and display their sum.
#include <stdio.h>
int main()
{
    int i, j;
    printf("enter two numbers: ");
    scanf("%d", &i, &j);
    printf("the sum of the two numbers is: %d", i + j);
    return 0;
}